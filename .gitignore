# Dependencies
node_modules/

# Build output
dist/
app/web/dist/

# Environment / secrets
.env
.env.*
!.env.example

# Runtime data
data/

# Logs
*.log
npm-debug.log*

# Vendored TrueForge checkout (1.2GB, upstream repo — not ours to track)
trueforge/

# Python bytecode
__pycache__/
*.pyc

# Local config (may hold endpoints and keys) — the .example files are the committed shape
config/*.json
!config/*.example.json
